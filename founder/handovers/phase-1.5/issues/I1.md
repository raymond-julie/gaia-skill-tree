# I1 — Schema: Trust Magnitude + 10-type taxonomy + apex gate

**Branch:** `schema/g7-trust-magnitude` • **Agent:** Sonnet 4.6 • **Lane:** A (parallel with I2) • **Effort:** M
**Source:** `founder/handovers/G7_TRUST_TAXONOMY_RFC.md` (RFC) + `founder/handovers/G7_IMPLEMENTATION_HANDOVER.md` §3 I1 + `founder/handovers/G7_HANDOVER_DELTA_2026-06-17.md` §A + §C.

## Definition of Done

- [ ] `registry/schema/skill.schema.json` adds `trustMagnitude` (number, ≥0), `overallTrustGrade` (enum `S|A|B|C|ungraded`), `apexGateStatus` (object — see below), `provisional` (boolean, default false), `provisionalUntil` (date, optional). `evidence[]` items add `grade` (enum `S|A|B|C`), `sourceStartedAt` (ISO date, optional). Add `links.canonicalRepo` (uri, optional). Add `cosigners` (array of `{username, org, signedAt}`, optional).
- [ ] `registry/schema/namedSkill.schema.json` mirrors the same additions.
- [ ] `registry/schema/meta.json` adds `trustMagnitudeThresholds: {S: 250, A: 100, B: 50, C: 20}`. Replaces legacy `evidence.types` (`arxiv`, `repo`, `github-stars`) with the 10-type taxonomy from RFC §2.1, each with `magnitude`, `weight`, `cap`, `gradeCeiling`, `freshness`. Adds top-level `apexGate` block with the 6 active predicates + `systemWideCap: 5` (CI-only). Removes `alternativePathways."6★".apexPath` (legacy). Bumps `version`.
- [ ] Bundled mirror at `src/gaia_cli/data/registry/schema/{skill,namedSkill,meta}.json` synced byte-for-byte.
- [ ] `gaia validate` green on a clean checkout.
- [ ] PR description lists every new field + every deprecated field (kept for backwards compat, not removed).

## The amended apex gate — **6 active predicates** (delta §A)

`apexGateStatus` object holds these as booleans. Two more are scaffolded for future activation but excluded from the gate evaluation today.

| Predicate | Active | Notes |
|---|---|---|
| `aGradedOriginsGte5` | ✅ | Consolidated origin-count predicate: ≥5 distinct A-or-S-graded origin evidence rows (subsumes the old `transitiveOriginsGte12` + `aGradedClosureGte8`). |
| `sourceTenureDaysGte180AorS` | ✅ | A/S-tier rows only; max source-age across those rows ≥180d. Reads new per-row `sourceStartedAt`. Replaces `tenureDaysGte180`. |
| `directNestedSuiteGte1` | ✅ | Unchanged. |
| `depth2OnlyReachableGte1` | ✅ | **Walk uses fusion graph (`role='origin'` filter), suite components excluded.** |
| `overallGradeS` | ✅ | Unchanged. |
| `apexPromotionPrSigned` | ✅ | Unchanged. |
| `crossOrgVerifierGte2` | 🚫 OFF | Feature-flagged OFF; predicate function returns `None` (skipped). Re-enabled at 2026-Q4 review per Decision D. |
| `systemWideCapRespected` | 🚫 OFF | Feature-flagged OFF in the per-skill object. Top-level `apexGate.systemWideCap: 5` lives in I4 CI only. |

Removed predicate names (do NOT add to schema): `transitiveOriginsGte12`, `aGradedClosureGte8`, `tenureDaysGte180`.

## Per-row `sourceStartedAt` (delta §C-1)

```json
"sourceStartedAt": {
  "type": "string",
  "format": "date",
  "description": "ISO date the source project/repo/publication was first created or published."
}
```

- Optional for B/C/ungraded rows.
- **Required at A/S tier for apex eligibility** — `gaia validate` warns (does NOT hard-fail) when an A/S row is missing this field. Hard-fail is I4's job post-cutover.
- Migration script (I3) flags any A/S row missing this with `provisional: true`.

## `role='variant'` enforcement (delta §C-2)

Add a meta.json validation rule (or a `gaia validate` check delegated to I2) so that:

> If an evidence row's referenced skill (or any skill in a fusion recipe's `origins` list) has `role: 'variant'` in the named index, that row contributes **zero magnitude** to fusion-recipe scoring.

I1 itself only declares the rule in `meta.json`; the actual zero-magnitude logic lives in I2's `computeArtifactScore()`.

## Constraints

- Do NOT remove the legacy `class` or `trustNumber` fields from `evidence[]` items — keep them as deprecated for backwards compat (RFC §5). Add `grade` as the primary new field; `class` becomes a fallback.
- Ten evidence types (final, RFC §2.1): `fusion-recipe`, `github-stars-own`, `proxy-containment`, `verifier-attestation`, `benchmark-result`, `arxiv`, `peer-review`, `repo-own`, `self-attestation`, `social-signal`.
- All new symbols use camelCase. No underscores in new field names.
- Must not touch any Hermes-owned file (`STEWARDSHIP_PLAN.md`, `scripts/marketing_engine.py`, `scripts/email_sender.py`, `scripts/share_deliverable.py`, `scripts/generate_adoption_dashboard.py`, `scripts/generate_showcase.py`, `docs/ADOPTION.html`, `docs/SHOWCASE.html`, `docs/WHY-GAIA.md`, `docs/QUICKSTART.md`).

## Verification

1. `python scripts/sync_bundled_schemas.py` if it exists; otherwise manual `cp` and document the gap.
2. `gaia validate` green on a clean checkout.
3. Empty PR sanity test: `gaia validate` on a registry with one named skill that has only the new fields populated.

## Closes by supersession

This PR closes **#654** (RFC: Evidence types — expand beyond arxiv / repo / stars). Orchestrator drafts the closing comment for Marco's approval pointing at `G7_TRUST_TAXONOMY_RFC.md` §3-§7 + this PR.

## Token-spend log

Comment on this issue at session end with model + tokens (per `CLAUDE.md` Token Spend Logging directive). Format: `<date> <model> <effort>: Xk in, Yk out. ~$Z`.
