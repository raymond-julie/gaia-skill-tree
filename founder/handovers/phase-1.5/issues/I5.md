# I5 — Apex cutover: demote `mattpocock/skills` + `ruvnet/ruflo`

**Branch:** `review/meta/g7-apex-cutover` • **Agent:** Sonnet 4.6 • **Lane:** D • **Effort:** S
**Blocked by:** I3 (writes `apexGateStatus`) + I4 (gate prevents re-promotion in the gap).

## Definition of Done

- [ ] `registry/named/mattpocock/skills.md` frontmatter `level: 6★ → 5★`. Add `provisional: true` + `provisionalUntil: 2026-12-17` (180-day grace, RFC §10.13).
- [ ] `registry/named/ruvnet/ruflo.md` same.
- [ ] Both skill timelines append a `demote` event via `gaia dev timeline --action demote --notes "G7 cutover — apex gate failed predicates [list per amended §A]; re-application open immediately"`.
- [ ] `registry/named-skills.json` regenerated.
- [ ] System-wide 6★ count = 0 after merge.

## Workflow

1. Use `gaia dev reclassify <skill> --level 5★` if CLI supports level changes. (CLAUDE.md flags this gap.) If not, direct frontmatter edit + `gaia dev timeline --user mbtiongson1 --action demote --notes "..."`. Document the gap in PR description.
2. Apply `apex-promotion` label to the PR (cutover IS an apex-state change). **Confirm with Marco before applying** — alternative is `skip-meta-guard`. Marco's call.
3. Stamp report from I3 must already be live before this merges so users see the announcement first.

## Verification

1. After merge, both skills show `level: 5★` on `gaiaskilltree.com/named/`.
2. Stamp report from I3 is the lead announcement.
3. System-wide 6★ count = 0; Job B baseline asserted.

## Token-spend log

Comment on this issue + on the merged PR.
