# I6 — Display layer: TM badge + named-index gen + report cards

**Branch:** `design/g7-tm-display` • **Agent:** Sonnet 4.6 • **Lane:** E (parallel to I7) • **Effort:** M • **Blocked by:** I3.

> 🛑 **HOLD before merge — Marco visually inspects.** This PR is design-surface; per orchestrator instruction 2026-06-17 the agent must signal "ready for visual inspection" and STOP before merge. Marco may file extensive comments.

## Definition of Done

- [ ] `scripts/generateNamedIndex.py` writes `trustMagnitude` per entry in `registry/named-skills.json` and `docs/graph/named/index.json`.
- [ ] `src/gaia_cli/treeManager.py` `show_tree()` surfaces TM number alongside the level glyph (e.g. `★★★★ A · TM 145`). Honor `--canon` flag.
- [ ] `docs/named/report.html` adds a Trust Magnitude card (after the Tenure card) showing TM, overall grade, and the **6 active apex-gate predicates** as a checklist (skipped predicates render with a "feature-flagged off — 2026-Q4 review" tag).
- [ ] `docs/named/index.html` (Skill Explorer modal) adds the same TM card. Reuse `_safeRender` wrapper from PR #714.
- [ ] `docs/css/styles.css` adds `.tm-badge` + `.apex-gate-checklist` rules, reusing `.report-card`/`.grade-bar` token vocabulary. **No hex literals — design tokens only.**
- [ ] `docs/evidence/` filter chips reconciled with real `grade` values across the registry.

## Verification (CLAUDE.md skill-explorer rule)

After any edit to `docs/js/skill-explorer.js` or `docs/named/index.html`, manually open `https://gaiaskilltree.com/named/` (or local), click any 2★+ skill, confirm all sections render: **Hero, Installation, Documentation, Upgrade Path, Evolution Changelog, + new TM card**. Topbar **Skill.md / Repo / Report / Skill Tree** buttons all open. DevTools console shows no `[skill-explorer]` warnings.

## Constraints

- Match `docs/DESIGN.md`. No new color hex literals; reuse CSS custom properties.
- Vocabulary: never "rarity". Use "Trust Magnitude" / "Apex" / "Origin" / "Variant" / "Suite Components" / "Trust Grade" per `CONTEXT.md`.

## Token-spend log

Comment on this issue + on the merged PR.
