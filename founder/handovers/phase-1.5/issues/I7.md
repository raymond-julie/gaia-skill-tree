# I7 — Codex Methodology Page (completion pass)

**Branch:** `docs/g7-trust-methodology` • **Agent:** Sonnet 4.6 • **Lane:** E (parallel to I6) • **Effort:** S–M (page already drafted in PR #716; this completes the gaps).
**Source:** `G7_HANDOVER_DELTA_2026-06-17.md` §D + current state of `docs/codex/trust-methodology.html`.

> 🛑 **HOLD before merge — Marco visually inspects.** Design-surface PR.

## Status as of dispatch

`docs/codex/trust-methodology.html` was shipped in PR #716 (`cbfd0d47`) — 40KB / 963 lines. Content is mostly delta-aligned (6-predicate gate ✓, `aGradedOriginsGte5` ✓, `sourceTenureDaysGte180AorS` ✓, `role='variant'` ✓, mattpocock 3/6 worked example ✓). PR #717 (`e7cfbe7f`) fixed the codex-toc nav→div rendering bug.

**Three gaps left for I7:**

## Definition of Done

- [ ] **Gap 1 — Page is orphaned.** Add an entry-point to the methodology page from at least one of:
  - `docs/codex.html` — primary Codex landing (1089 lines, currently has no link to `docs/codex/trust-methodology.html`).
  - `docs/index.html` Hero CTA pill — currently points at the 06-15 meta report; add or replace with a Trust Methodology link.
  - `docs/badges/` page — Trust Magnitude is computed there; user-facing reference.
  Recommendation: add a card on `docs/codex.html` matching the existing card pattern. Card title: "Trust Methodology". Subtitle: "How Trust Magnitude, grades, and the Apex gate work."
- [ ] **Gap 2 — Stray references to removed predicates.** Two mentions of `transitiveOriginsGte12` etc. in the page. Verify they are correctly framed as "removed in 2026-06-17 amendments" (historical context) and not as active predicates.
- [ ] **Gap 3 — TM values are draft, not post-migration.** The mattpocock/skills worked example uses pre-migration TM values. After I3 lands, this PR rebases and updates the worked example to read from the post-migration registry (`docs/graph/named/index.json` or `registry/named-skills.json`).

## Constraints (delta §D)

- Match `docs/DESIGN.md` exactly. **No color hex literals**; reuse CSS custom properties already defined in `docs/css/tokens.css`.
- Vocabulary: never "rarity". Use "Trust Magnitude" (not "Trust Score"/"Weight"/"Reputation"). "Apex" (not "6-star" in prose; glyphs OK). "Origin" / "Variant" / "Suite Components" / "Trust Grade" per `CONTEXT.md`.
- Hermes-owned files must NOT be touched.

## Verification

1. `grep -i rarity docs/codex/trust-methodology.html` → 0 results.
2. Page opens without JS errors; in-body `.codex-toc` renders inline (PR #717 fix preserved).
3. All 10 evidence type rows render in the table.
4. The 6-predicate apex gate (3 active + 3 active = 6, plus 2 OFF flagged) renders with correct labels.
5. Worked example uses TM values matching the merged migration output (when I3 lands).
6. Card on `docs/codex.html` links correctly and matches the existing card component.

## Cross-link

I6 (Display layer) and I7 (this) are both on Lane E and both gated for visual inspection. Marco may stack comments across both PRs.

## Token-spend log

Comment on this issue + on the merged PR.
