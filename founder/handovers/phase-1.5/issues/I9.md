# I9 — Big-Bang Follow-Up: Evidence Backfill + Type Cleanup

**Branch:** `review/meta/g7-evidence-backfill` • **Agent:** Sonnet 4.6 • **Lane:** F (after I3 merges to main) • **Effort:** L
**Depends on:** I1 ✅, I2 ✅, I3 (PR #733 — merge before dispatching I9)

---

## Objective

I3 (PR #733) ran `computeTrustMagnitude` over all 235 named skills and got TM=0 for every skill because:

1. **All 174 evidence rows use `type: repo`** — the pre-G7 legacy name, not recognized by the G7 scorer which knows `repo-own`.
2. **Zero rows of any other G7 type** exist in any named skill frontmatter.
3. **Multiple evidence types exist in the data lake** (founder/sources/) but have NOT been ingested into the registry.

This issue tracks the follow-up pass to get real non-zero TM values across the registry.

---

## Fix 1 — Scorer alias: `repo` → `repo-own` (CLI, 1 line)

Add `repo` as an accepted alias for `repo-own` in `src/gaia_cli/trustMagnitude.py`. The user's intent: `repo` and `repo-own` are the same concept. `proxy-containment` handles the external-repo case.

```python
# In TYPE_WEIGHTS or the evidence scoring loop:
TYPE_ALIASES = {'repo': 'repo-own'}
# Before scoring: effective_type = TYPE_ALIASES.get(row.type, row.type)
```

This unblocks 174 existing rows immediately without touching any registry data.

**Do NOT rename the 174 frontmatter rows** — `repo` stays valid.

---

## Fix 2 — Evidence type cleanup per skill (registry, `review/meta/` scope)

### 2a — Star counts: add `github-stars-own` rows

Skills whose evidence notes mention star counts but only have `type: repo`:

| Skill | Stars | Mothership? | Expected m |
|---|---|---|---|
| `obra/superpowers` | 230,818 | Yes (11 sub-skills → divisor=4) | 50 |
| `mattpocock/skills` | 133,210 | Yes (19 sub-skills → divisor=4) | 33 |
| `garrytan/gstack` | 110,930 | Yes (47 sub-skills → divisor=4) | 28 |
| `safishamsi/graphify` | 68,766 | No (standalone) | 69 |
| `pbakaus/impeccable` | 38,000 | No (standalone) | 38 |
| `addy-osmani/performance-optimization` | 47,200 | No (standalone) | 47 |
| `ruvnet/ruflo` | 59,957 | Yes (47 sub-skills → divisor=4) | 15 |

Add a separate `github-stars-own` evidence row alongside the existing `repo` row. Do NOT remove the `repo` row.

### 2b — `class: A / grade: B` mismatch — fix 2 rows

- `addy-osmani/performance-optimization`: `class: A` but `grade: B`. Grade should be A (trustNumber 70 → grade B is correct by raw threshold but class A was manually assigned by evaluator as higher quality). Align: keep `grade: B` as computed, but add note that class A assignment is preserved.
- `ruvnet/ruflo`: same pattern — `class: A`, `grade: B`. Same resolution.

### 2c — arxiv evidence from data lake (12 skills)

Source: `founder/sources/collectors/technical/academic_papers.md`

Add `type: arxiv` evidence rows to the following named skills. Use `gaia dev evidence <skillId> --class A` for each — these are all Class A (academic paper):

| Skill | ArXiv ID | Citations (approx) | Expected m |
|---|---|---|---|
| `anthropic/skill-creator` | 2305.17126 | 300+ | 60 (capped) |
| `stanfordnlp/dspy` | 2310.03714 | 700+ | 100 (capped) |
| `mattpocock/skills` | 2602.20867 | 52 | 10 |
| `devin-ai/autonomous-swe` | 2503.12374 | ~50 | 10 |
| `safishamsi/graphify` | 2408.03910 | 89 | 18 |
| `browser-use/browser-harness` | 2601.07263 | ~5 | 1 |
| `firecrawl/firecrawl` | 2508.10152 | ~0 | 0 (too new) |
| `pbakaus/impeccable` | 2411.01606 | 19 | 4 |
| `ruvnet/ruflo` | 2602.06547 | ~20 | 4 |
| `garrytan/gstack` | 2606.09832 | ~0 | 0 (too new) |
| `obra/superpowers` | 2605.17242 | ~5 | 1 |
| `sickn33/mcp-builder` | 2506.13538 | ~0 | 0 (too new) |
| `upsonic/unittest-generator` | 2403.16218 | 15 | 3 |

Verify each arxiv URL is live before adding. Source: `founder/sources/collectors/verification/firecrawl_validation_report_2026_06_19.md`.

### 2d — `links.arxiv` → evidence row conversion (2 skills)

- `openai/few-shot-learning`: `links.arxiv: https://arxiv.org/abs/2005.14165` — convert to `evidence:` row with `type: arxiv`
- `openai/self-consistency`: `links.arxiv: https://arxiv.org/abs/2203.11171` — same

### 2e — Body-text evidence → frontmatter (2 skills)

- `pexp13/sentiment-analysis`: has 8-row Evidence table in markdown body, not frontmatter. Convert the arxiv and ACL rows to frontmatter evidence rows (`type: arxiv` for arxiv.org sources, `type: peer-review` for ACL/SemEval).
- `stanfordnlp/dspy`: arxiv link in prose, no frontmatter evidence array. See 2c above.

### 2f — Social signal (highest value only, for now)

`garrytan/gstack`: Y Combinator YouTube video → `type: social-signal`, `creator_mult: topicalAuthority (1.0)`.
Source: `founder/sources/collectors/social/youtube_showcases.md`.

Only ingest the YC video for now. Other social signals (obra/superpowers via CodeRabbit, safishamsi/graphify via Charlie Automates) are deferred to a future pass.

### 2g — Zero-evidence skills: add minimal `self-attestation` row

See P6 list in `founder/handovers/phase-1.5/P6_ZERO_EVIDENCE_SKILLS.md`. Add `type: self-attestation` for each — this gives a floor C score and makes them visible in the scorer. **Do not fabricate star counts or citations that cannot be verified.**

---

## Fix 3 — Re-run migration after fixes 1+2

After all CLI evidence additions land:
1. Run `python scripts/migrateTrustMagnitude.py` to recompute TM for all 235 skills
2. Regenerate `registry/named-skills.json` (run `python scripts/generateNamedIndex.py`, then revert noise files per hazard rule #9)
3. Regenerate `docs/graph/named/index.json` (required by CI)
4. Verify TM is non-zero for the affected skills
5. Commit stamp report update

---

## Acceptance criteria

- [ ] `type: repo` accepted by scorer (alias to `repo-own`) — no TM=0 on existing rows
- [ ] `github-stars-own` rows added for the 7 star-rich skills (2a)
- [ ] 2 class/grade mismatches documented (2b)
- [ ] Arxiv evidence rows added for ≥8 of 13 skills (skip 0-citation papers, 2c)
- [ ] `openai/few-shot-learning` + `openai/self-consistency` evidence arrays created (2d)
- [ ] `pexp13/sentiment-analysis` body evidence promoted to frontmatter (2e)
- [ ] `garrytan/gstack` YC social-signal row added (2f)
- [ ] `python scripts/migrateTrustMagnitude.py` runs clean, TM > 0 for affected skills
- [ ] CI passes (no version bump)
- [ ] Branch: `review/meta/g7-evidence-backfill`. Label: `skip-scope-check`

---

## Token-spend log

Comment on this issue and the merged PR.
